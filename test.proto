syntax = "proto3";
package comm;
option go_package = "github.com/kmrd-industries/qlp-proto-bindings/test/go";

enum State {
    STATE_UNSPECIFIED = 0;
    STATE_ALIVE = 1;
    STATE_DEAD = 2;
}

message Data {
    optional int32 i = 1;
    optional string a = 2;
    State state = 3;
    repeated string msg_list = 4;
}

